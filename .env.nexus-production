# Nexus Multi-Channel Platform - Production Environment Configuration
# Copy to .env and customize for your deployment

# Project Configuration
PROJECT_NAME=nexus-platform
BUILD_VERSION=1.0.0
BUILD_DATE=2025-08-03
BUILD_REVISION=main
ENVIRONMENT=production

# Domain Configuration
DOMAIN=nexus.example.com
ACME_EMAIL=admin@example.com

# Nexus Platform Configuration
NEXUS_ENABLE_API=true
NEXUS_ENABLE_CLI=true
NEXUS_ENABLE_MCP=true
NEXUS_API_PORT=8000
NEXUS_MCP_PORT=3001

# Authentication & Security
NEXUS_JWT_SECRET=your-super-secret-jwt-key-here-256-bits-minimum
NEXUS_API_KEY=your-nexus-api-key-here
SECRET_KEY=your-django-secret-key-here-keep-it-secret
ENCRYPTION_KEY=your-encryption-key-here-32-bytes

# Database Configuration
POSTGRES_PASSWORD=your-secure-postgres-password-here
DATABASE_URL=postgresql://nexus_user:${POSTGRES_PASSWORD}@postgres:5432/nexus_db
DATAFLOW_DATABASE_URL=postgresql://nexus_user:${POSTGRES_PASSWORD}@postgres:5432/nexus_db

# Redis Configuration
REDIS_URL=redis://redis:6379/0

# Neo4j Configuration
NEO4J_USER=neo4j
NEO4J_PASSWORD=your-secure-neo4j-password-here
NEO4J_URI=bolt://neo4j:7687

# AI Services Configuration
OPENAI_API_KEY=your-openai-api-key-here
OPENAI_MODEL=gpt-4
CLASSIFICATION_CONFIDENCE_THRESHOLD=0.8

# MinIO Configuration
MINIO_ROOT_USER=admin
MINIO_ROOT_PASSWORD=your-secure-minio-password-here

# Monitoring Configuration
GRAFANA_ADMIN_PASSWORD=your-secure-grafana-password-here

# Traefik Configuration
TRAEFIK_BASIC_AUTH=admin:$2y$10$your-bcrypt-hash-here

# Performance Configuration
NEXUS_WORKERS=4
NEXUS_TIMEOUT=300
CACHE_TTL=300
MAX_CONNECTIONS=100
POOL_SIZE=20

# Security Configuration
CORS_ORIGINS=https://app.nexus.example.com,https://dashboard.nexus.example.com
ENABLE_RATE_LIMITING=true
RATE_LIMIT_PER_MINUTE=100
SECURITY_HEADERS_ENABLED=true

# Monitoring & Logging
ENABLE_METRICS=true
METRICS_PORT=9090
HEALTH_CHECK_INTERVAL=30
LOG_LEVEL=info
ENABLE_STRUCTURED_LOGGING=true

# Production Optimization
PYTHONOPTIMIZE=1
PYTHONUNBUFFERED=1
WORKERS_PER_CORE=1
MAX_WORKER_CONNECTIONS=1000
WORKER_CLASS=uvicorn.workers.UvicornWorker

# Resource Limits (for docker-compose)
NEXUS_MEMORY_LIMIT=4G
NEXUS_CPU_LIMIT=2.0
POSTGRES_MEMORY_LIMIT=4G
POSTGRES_CPU_LIMIT=2.0
REDIS_MEMORY_LIMIT=2G
REDIS_CPU_LIMIT=1.0