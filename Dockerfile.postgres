# PostgreSQL with pgvector extension for AI embeddings and semantic search
# Using pre-built image to avoid compilation issues
FROM ankane/pgvector:v0.5.1

# Add production-ready configuration
ENV POSTGRES_INITDB_ARGS="--encoding=UTF8 --locale=C"

# Add healthcheck
HEALTHCHECK --interval=10s --timeout=5s --retries=5 \
    CMD pg_isready -U ${POSTGRES_USER:-postgres} || exit 1

# Expose PostgreSQL port
EXPOSE 5432

# Use default postgres entrypoint and healthcheck from base image
